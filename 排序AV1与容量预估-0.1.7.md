# 排序、AV1 与容量预估 0.1.7

## 文件顺序

导入后点击“文件名升序”或“文件名降序”，对整个输入列表按文件名自然排序，例如第1集、第2集、第10集。同名文件保留原来的相对顺序。

选中一个或多个视频，点击“移动到序号…”，输入移动后第一项的位置（从 1 开始）。多选文件按当前相对顺序一起移动；原有上移、下移和拖动排序仍可使用。列表左侧显示序号，任务按此列表顺序取输入。已加入队列的任务保留加入时的顺序。

## AV1

在“编码”选择 AV1，软件自动切换自定义预设，防止 H.264 模板覆盖选择。高级设置可指定编码器：软件 `libsvtav1` 或通过实际自检的 `av1_nvenc`。质量参数允许 0～63（NVENC 的 CQ=0 表示自动）；不同编码器相同数值不代表相同画质或体积。

启动时真实试编码并检查 NVIDIA 每张卡的 AV1 能力。两卡均通过且满足原有双卡条件时，复用双进程分段和复制视频流拼接流程。单路 AV1 硬件编码初始化失败时，回退软件 AV1，保留输出编码格式。支持 MP4/MKV、配置保存及导入导出。HDR 保留模式仍明确使用 HEVC Main10。

SVT-AV1 的快速/均衡/精细对应 preset 10/8/6，使用 4 个逻辑处理器以限制后台编码资源。NVIDIA 复用现有 p4/p5/p6 与 VBR/CQ 参数。

## 容量预估

改变参数后自动刷新“预计输出总容量”。独立压缩显示所有输出的合计容量。

- 流复制：按输入大小合计估算，封装变化仍可能改变大小。
- 目标码率或目标容量：按视频码率、适用的音频码率和时长计算，预留约 2% 封装开销；单遍码率控制可能偏离目标。
- 恒定质量：停改约 1 秒后，自动在总时间线 20%、50%、80% 附近各抽取最多 2 秒，用当前编码、质量、速度、尺寸和帧率实际试编码。根据视频包大小及音频码率外推总容量。样本自动删除，不修改原片。

三段样本无法覆盖所有剧情或动态场景，短片的关键帧比例也可能高于整片；结果为粗估，不能承诺误差范围或压缩比例。每段编码最多等待 20 秒。参数改变后忽略旧结果，并在下一段开始前停止过期估算；正在执行的短样本会先结束。正式任务运行期间不启动新估算，队列空闲后恢复。试编码失败会显示提示，可切换码率模式获得即时估算。

## 开源复用

没有新增第三方依赖或复制其他压缩器的整套调度代码。排序直接使用已安装 Qt 的 QCollator 数字自然排序；AV1 使用内置 FFmpeg 的现成 SVT-AV1/NVENC 编码器；双卡调度、参数生成、配置和输出校验复用本项目已有实现。参考：

- [Qt QCollator 官方文档](https://doc.qt.io/qt-6/qcollator.html)
- [FFmpeg AV1 官方参数](https://ffmpeg.org/ffmpeg-codecs.html#libsvtav1)
- [FFmpeg 开源 NVENC AV1 实现](https://github.com/FFmpeg/FFmpeg/blob/master/libavcodec/nvenc_av1.c)

## 验证

73 项回归测试通过：包含自然升降序、多选移动与选择保留、参数变动后估算刷新、AV1 配置往返、真实 AV1 MP4/MKV 转码及完整解码、双进程 AV1 分段拼接帧数/时间戳/解码检查、模拟 GPU 故障后实际软件 AV1 回退，以及原有取消和任务操作测试。

打包程序自检包含 13 项：原有 10 项，加 AV1 软件编码、自然排序、样本容量估算。本机没有 NVIDIA 硬件；双 GPU 设备绑定以模拟设备测试，媒体流水线使用真实软件编码验证。未声称完成 RTX 4070 双卡 AV1 硬件吞吐测试，需在目标机安装新版后通过逐卡自检并验证实际速度。
